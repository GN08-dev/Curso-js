//texto = 'cadena de texto'; //esto es un string o cadena de texto
//numero = 19;
//boleanos = false; // se usa true(0) i false(1)
//casos especiales de variables no definidas 
//formas de  defenir una variables es con 

//let , var, cosnt

// let se usa para limitar el bloque dode se esta ejecutando 
/**es decir si dentro de un bloque del if se dejeclara un let
 * unicamente dentro del if se puede ser accesible la variable 
 * mientras que var se puede usar donde sea de manera global
 * y existe las constantes 
 */

//declaramos una variable
let numero = 16;
let result = numero * 8 ;
result *= 2;
alert(result)
//un valor undefined es cuando hay una variable asignada pero que 
// no contiene ningun valor dentro 

// cuando se le coloca una variable null, seginifica que estara vacia 
//nan sginifica que no es un numero cuando quiere uno realizar una suma con
// una cadena o alguna operacion con algo que no es un numero 

//como crear multiples variables 

